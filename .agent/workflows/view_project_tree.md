---
description: Visualize the project file structure (excluding node_modules and docs)
activation: manual
---

1. Execute the project tree script.
   Note: By default, this generates a Markdown file. Use `-t` for terminal-only output or `-n` for structure only (no content). You can also specify a target path.
   Command: `python scripts/doc_project.py -t`
